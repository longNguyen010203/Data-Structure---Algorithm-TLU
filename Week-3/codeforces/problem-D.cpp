#include <iostream>
#include <vector>
using namespace std;



int main() {

    unsigned int n; cin >> n;
    int a[n];
    for (int i = 0; i < n; i++) cin >> a[i];

    unsigned int k; cin >> k;

    vector<int> v;
    int l, r;

    for (int i = 0; i < k; i++) {
        cin >> l >> r;
        v.push_back(l);
        v.push_back(r);
    }

    for (int i = 0; i < n; i++) {
        int count = 0;
        if (a[i] >= )
    }


    return 0;
}