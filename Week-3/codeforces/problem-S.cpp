#include <iostream>
#include <map>
#include <string>
using namespace std;


int main() {

    unsigned int n; cin >> n;
    map<string, int> mp;

    for (int i = 0; i < n; i++) {
        string request; cin >> request;
        // if (mp.find(request)) {}
    }
        
    return 0;
}